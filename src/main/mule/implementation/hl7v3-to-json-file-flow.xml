<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:file="http://www.mulesoft.org/schema/mule/file"
	xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
		http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
		http://www.mulesoft.org/schema/mule/file http://www.mulesoft.org/schema/mule/file/current/mule-file.xsd">

	<!--
		HL7 v3 is an unrelated wire format from HL7 v2 (RIM-based XML documents, e.g. CDA),
		not something the HAPI HL7v2 library (used for the v2 flows) reads at all. It is
		already well-formed XML, though, so no HAPI/Java code is needed: DataWeave's own
		XML reader does the whole job.

		Polls hl7v3.input.directory for *.xml files, converts each to JSON, writes
		hl7v3.output.directory/<name>.xml.json, and moves the source file to
		hl7v3.processed.directory.
	-->
	<flow name="hl7v3-to-json-file-flow" doc:name="hl7v3-to-json-file-flow">

		<file:listener doc:name="On new HL7 v3 file" config-ref="HL7_File_Config"
			directory="${hl7v3.input.directory}"
			moveToDirectory="${hl7v3.processed.directory}"
			autoDelete="false">
			<scheduling-strategy>
				<fixed-frequency frequency="${hl7v3.poll.frequencyMillis}" />
			</scheduling-strategy>
			<file:matcher filenamePattern="*.xml" />
		</file:listener>

		<set-variable variableName="sourceFileName" value="#[attributes.fileName]" doc:name="Save source file name" />

		<try doc:name="Try">
			<ee:transform doc:name="Convert HL7 v3 XML to JSON">
				<ee:message>
					<ee:set-payload><![CDATA[%dw 2.0
output application/json
---
read(payload, "application/xml")]]></ee:set-payload>
				</ee:message>
			</ee:transform>

			<file:write doc:name="Write JSON output" config-ref="HL7_File_Config"
				path="#['${hl7v3.output.directory}/' ++ vars.sourceFileName ++ '.json']"
				mode="OVERWRITE" />

			<logger level="INFO" doc:name="Log success"
				message="#['Converted ' ++ vars.sourceFileName ++ ' to JSON successfully']" />

			<error-handler>
				<on-error-continue doc:name="Log and continue">
					<logger level="ERROR" doc:name="Log failure"
						message="#['Failed to convert ' ++ (vars.sourceFileName default 'unknown file') ++ ': ' ++ error.description]" />
				</on-error-continue>
			</error-handler>
		</try>

	</flow>

</mule>
